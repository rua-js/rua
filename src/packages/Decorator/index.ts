import Listen from './Listen'
import Connect from './Connect'
import Instance from './Instance'
import API from './API'

const Decorator = {
  Listen,
  Connect,
  Instance,
  API,
}

export {
  Decorator,
}
